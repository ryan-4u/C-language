#include<stdio.h>
int main() 
{
    int a;
    float b;
    char c;
    printf("\n\n\t\tSize of int: %d bytes\n", sizeof(a));
    printf("\t\tSize of float: %d bytes\n", sizeof(b));
    printf("\t\tSize of char: %d bytes\n\n\n", sizeof(c));
    return 0; 
}
